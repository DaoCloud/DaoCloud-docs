{% extends "forms/field.html.twig" %}

{% block input %}
    <div class="form-display-wrapper {{ field.size }}">
        {% if field.markdown %}
            {{ field.content|t|markdown }}
        {% else %}
            {{ field.content|t }}
        {% endif %}
    </div>
{% endblock %}
