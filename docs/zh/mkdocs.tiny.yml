# This file is used to split config and docs navigation
# Project information
site_name: DaoCloud 文档中心

# custom module path
docs_dir: docs/middleware/

# Configuration
theme:
  name: material

  # navigation config
  features:
    - navigation.tabs
